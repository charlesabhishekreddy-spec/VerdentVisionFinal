import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function FarmPreferences() {
  return (
    <Card>
      <CardHeader><CardTitle>Farm Preferences</CardTitle></CardHeader>
      <CardContent>User-editable farm profile and notifications settings.</CardContent>
    </Card>
  )
}
