import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function DiagnosisResult() {
  return (
    <Card>
      <CardHeader><CardTitle>Diagnosis Result</CardTitle></CardHeader>
      <CardContent>Plant disease result details, severity, and advice.</CardContent>
    </Card>
  )
}
