import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function WeatherInput() {
  return (
    <Card>
      <CardHeader><CardTitle>Weather Input</CardTitle></CardHeader>
      <CardContent>Manual weather log entry form for local records.</CardContent>
    </Card>
  )
}
