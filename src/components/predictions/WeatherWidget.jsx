import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function WeatherWidget() {
  return (
    <Card>
      <CardHeader><CardTitle>Weather Widget</CardTitle></CardHeader>
      <CardContent>Compact weather block for summary views.</CardContent>
    </Card>
  )
}
