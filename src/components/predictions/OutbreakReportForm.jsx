import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function OutbreakReportForm() {
  return (
    <Card>
      <CardHeader><CardTitle>Outbreak Report Form</CardTitle></CardHeader>
      <CardContent>Form for reporting new outbreaks with location and image.</CardContent>
    </Card>
  )
}
