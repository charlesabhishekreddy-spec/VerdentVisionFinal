import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function PostList() {
  return (
    <Card>
      <CardHeader><CardTitle>Post List</CardTitle></CardHeader>
      <CardContent>Forum feed with sorting/filtering and metadata.</CardContent>
    </Card>
  )
}
