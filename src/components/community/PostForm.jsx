import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function PostForm() {
  return (
    <Card>
      <CardHeader><CardTitle>Post Form</CardTitle></CardHeader>
      <CardContent>Create forum posts with category/tags and media.</CardContent>
    </Card>
  )
}
