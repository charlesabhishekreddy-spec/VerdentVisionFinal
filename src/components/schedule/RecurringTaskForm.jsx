import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function RecurringTaskForm() {
  return (
    <Card>
      <CardHeader><CardTitle>Recurring Task Settings</CardTitle></CardHeader>
      <CardContent>Recurrence rules and end-date controls.</CardContent>
    </Card>
  )
}
