import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export default function PestResult() {
  return (
    <Card>
      <CardHeader><CardTitle>Pest Result</CardTitle></CardHeader>
      <CardContent>Identified pest details, confidence, and lifecycle notes.</CardContent>
    </Card>
  )
}
